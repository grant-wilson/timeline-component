import { TitleDefOutletDirective } from './title-def-outlet.directive';

describe('TitleDefOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleDefOutletDirective();
    expect(directive).toBeTruthy();
  });
});
