import { TimelineTitleDirective } from './timeline-title.directive';

describe('TimelineTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineTitleDirective();
    expect(directive).toBeTruthy();
  });
});
